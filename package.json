{
  "private": true,
  "scripts": {
    "test:visual": "cypress run --headless",
    "start:server": "serve -l 8000 .",
    "test": "start-server-and-test start:server 8000 test:visual"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "todomvc-app-css": "^2.4.2"
  },
  "devDependencies": {
    "@percy/cli": "^1.12.0",
    "@percy/cypress": "^3.1.2",
    "cypress": "^13.8.1",
    "cypress-image-diff-js": "^2.2.1",
    "serve": "^14.0.1",
    "start-server-and-test": "1.14.0"
  }
}
